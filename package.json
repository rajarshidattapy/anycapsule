{
  "name": "anyllm-browser",
  "version": "1.1.0",
  "description": "AnyLLM snapshots what happened and hands it to whatever comes next.",
  "main": "src/background.js",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "zip": "npm run build && cd dist && zip -r ../anyllm-browser-v1.1.0.zip . && echo 'Zipped: anyllm-browser-v1.1.0.zip'"
  },
  "devDependencies": {
    "vite": "^6.0.0",
    "vite-plugin-web-extension": "^4.0.0"
  }
}
